services:

    hd_secure:
        image: ghcr.io/jandolejs/hd_secure:latest
        build:
            context: .
        container_name: hd_secure_frontend
        restart: unless-stopped
        ports:
            - 8084:80
